<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Change Password</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/login/login.css">
</head>
<body>

  <div class="container">
    <div class="form-section">
      <div></div>
      
      <div class="form-content">
        <h1 class="title">
          New Password
        </h1>

        <form id="change">
          <div class="form-group">
            <label class="form-label" for="email">Email</label>
            <input 
              type="email" 
              id="email" 
              class="form-input" 
              placeholder="Example@email.com"
              required
            >
          </div>

          <div class="form-group">
            <label class="form-label" for="password">Password</label>
            <input 
              type="password" 
              id="password" 
              class="form-input" 
              placeholder="At least 8 characters"
              required
            >
          </div>

          <div class="form-group">
            <label class="form-label" for="password">Confirm your password</label>
            <input 
              type="password" 
              id="confirmePassword" 
              class="form-input" 
              placeholder="At least 8 characters"
              required
            >
          </div>

          <button type="submit" class="btn-primary">Confirm</button>
        </form>




        <p class="signup-text">
          Do you have an account? <a href="/sign-in" class="signup-link">sign-in</a>
        </p>
      </div>

  
    </div>

    <div class="image-section">
      <div class="image-wrapper">
        <img 
          src="/images/login.jpg" 
          alt="flower" 
          class="artwork-image"
        >
      </div>
    </div>
  </div>
  <script src="javascripts/newPassword/index.js"></script>
</body>
</html>